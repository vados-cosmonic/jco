{
  "name": "node-svcworker-fetch-event",
  "description": "Example of using a WebAssembly Component to answer requests via the ServiceWorker Fetch API with jco",
  "type": "module",
  "scripts": {
    "build:component": "jco componentize -w wit/component.wit component.js -o component.wasm",
    "transpile": "jco transpile -o dist/transpiled component.wasm",
    "serve": "http-server .",
    "demo": "npm run serve",
    "all": "npm run build:component && npm run transpile"
  },
  "dependencies": {
    "@bytecodealliance/jco": "^1.10.2"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
